import { Query } from "./resolver/query";
import { Mutation } from "./resolver/mutation";
import { Message } from "./resolver/messages";
import { Group } from "./resolver/groups";

export const resolvers = {
  Query,
  Mutation,
  Message,
  Group,
};
